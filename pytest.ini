[pytest]
# Restrict test discovery to the 'tests' directory
testpaths = tests

# Only consider files matching test_*.py as python test files
python_files = test_*.py

# Standard class and function patterns
python_classes = *Tests
python_functions = test_*

# Directories to ignore during discovery to prevent accidental scanning
norecursedirs = .git .venv build dist app core database
